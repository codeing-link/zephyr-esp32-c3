# SHLZ 蓝牙透传模块需求整理

> 本文根据 `蓝牙需求.txt` 整理为 Markdown，已删除公司地址、联系方式、编制单位及其他公司信息。

## 产品信息

|项目|内容|
|---|---|
|产品名称|SHLZ 蓝牙透传模块|
|产品型号|LSD1BT-STSHLZ02|
|文档版本|REV03|
|固件版本|V2.1|

## 功能特点

- 基于 ST BlueNRG-2 系列芯片的低功耗蓝牙 5.0 SoC 方案。
- 板载 PCB 天线，通过串口配置实现数据透传。
- 32 MHz 晶振、256 KB Flash、24 KB RAM、32 位 Cortex-M0。

## 规格参数

|参数|性能|备注|
|---|---|---|
|工作电压|2.1 V～3.6 V|典型值 3.3 V|
|工作温度|-40 ℃～85 ℃||
|存储温度|-40 ℃～85 ℃||
|工作频段|2400 MHz～2483.5 MHz||
|初始频偏|±30 kHz||
|发射功率|4 dBm|默认值，可由程序调整|
|发射电流|22 mA|典型值，3.3 V、4 dBm|
|接收电流|16.5 mA|典型值，3.3 V|
|接收灵敏度|-84 dBm|典型值|
|空旷传输距离|>15 m|0 dBm，仅供参考|
|外形尺寸|11.2 mm × 15.6 mm × 2.3 mm||

## 接口说明

|引脚|名称|方向|说明|芯片管脚|
|---:|---|---|---|---|
|1|GND|电源|模块电源负极|-|
|2|VCC|电源|模块电源 3.3 V|-|
|5|RST|输入|低电平复位，默认上拉|-|
|7|DIO10|I/O|程序下载 SWDIO|-|
|8|DIO9|I/O|程序下载 SWCLK|-|
|11|BOOT|I/O|高电平进入 Boot；正常复位时必须低电平或浮空|DIO7|
|12|DTM|I/O|高电平进入厂测；正常复位时必须低电平或浮空|DIO3|
|15|BCTS|输出|低电平表示模块有数据发送，高电平表示无数据发送|DIO2|
|16|TX|输出|模块串口发送端|DIO8|
|17|RX|输入|模块串口接收端|DIO11|

## 应用注意事项

1. 建议使用稳定的直流电源，可靠接地，注意电源极性。
2. 模块应位于底板边缘的空旷位置，天线朝外。
3. 天线下方 PCB 各层应净空，不应铺铜或布设信号线。
4. 天线附近避免放置金属器件。

## 透传协议

### 出厂系统参数和 BLE 协议

- 默认串口波特率：115200 bps。
- BLE 数据通道：服务 UUID `0xFFF0`，特征 UUID `0xFFF3`。
- 串口数据通道：服务 UUID `0xFFF0`，特征 UUID `0xFFF4`。
- 默认 MTU：23 字节，单包有效载荷最大为 `23 - 3 = 20` 字节。
- 蓝牙主机最大可设置 MTU：153 字节。

### 通用 AT 规则

- 以 `TTM` 开头的串口数据按 AT 指令解析；模块先原样回显命令，随后返回执行结果。
- 成功结果为 `TTM:OK\r\n\0`，参数或格式错误为 `TTM:ERP\r\n\0`。
- 不以 `TTM` 开头的串口数据按透传数据处理。
- BLE 连接成功后，TX 输出 `TTM:OK`；断开后，TX 输出 `TTM:DISCONNET`。

### AT 指令

#### 1. 波特率设定

```text
TTM:BPS-<baudrate>
```

- 支持：2400、4800、9600、14400、19200、28800、38400、57600、76800、115200。
- 模块先以旧波特率返回结果，再切换到新波特率。
- 默认值为 115200，掉电不保存。

#### 2. 模块复位

```text
TTM:RST-SYSTEMRESET
```

执行软件复位。

#### 3. 查询厂商 ID

```text
TTM:VID-?
```

返回：`TTM:VID-LSD_BLE_5.0\r\n\0`。

#### 4. 查询固件版本

```text
TTM:REV-?
```

返回：`TTM:REV-V2.1\r\n\0`。

#### 5. 数据延时设定

```text
TTM:CDL-<X>ms
```

- `X` 可为：0、2、5、10、15、20、25。
- 设置 BCTS 拉低至 TX 输出数据的延时；实际延时为 `(X + Y) ms`，其中 `500 us < Y < 1 ms`。
- 默认值为 0，掉电保存，仅未连接时可设置。

#### 6. 发射功率设定

```text
TTM:TPL-<X>
```

- `X` 可为：`+4`、`0`、`-6`、`-23`，单位 dBm。
- 默认值为 `+4 dBm`，掉电不保存。
- 已连接时设置会主动断开并重新广播。

#### 7. 广播周期设定

```text
TTM:ADP-<X>
```

- 广播周期为 `X × 100 ms`。
- `X` 可为：2、5、10、15、20、25、30、40、50。
- 文档描述出厂默认广播周期为 20 ms；设置项掉电保存，已连接时设置会主动断开并重新广播。

#### 8. 产品识别码

```text
TTM:PID-<Data>
```

- `Data` 为 2 字节产品识别码，范围 `0x0000`～`0xFFFF`。
- 识别码位于广播中，用于过滤或识别设备。
- 默认值 `0x0000`；掉电保存，已连接时设置会主动断开并重新广播。

#### 9. 查询广播名称

```text
TTM:REN-?
```

返回：`TTM:REN-<名称>\r\n\0`。

默认名称为 `LSD-BLE-yyyyyy`，其中 `yyyyyy` 为默认出厂 MAC 地址的后 3 字节。

#### 10. 模块重命名

```text
TTM:REN-<Name>
```

- 名称长度不超过 20 字节。
- 掉电保存，仅未连接时可设置。

#### 11. 获取物理地址 MAC

```text
TTM:MAC-?
```

返回：`TTM:MAC-xxxxxxxxxxxx\r\n\0`，其中 `xxxxxxxxxxxx` 为 6 字节蓝牙地址。

#### 12. 自定义 MAC 地址

```text
TTM:MAC-xxxxxxxxxxxx
```

- `xxxxxxxxxxxx` 为 6 字节 MAC 地址。
- 掉电保存，仅未连接时可设置。

#### 13. 查询附加自定义广播内容

```text
TTM:ADD-?
```

返回：`TTM:ADD-<内容>\r\n\0`。

文档描述默认值为 6 字节空数据：`0x000000000000`。

#### 14. 设置附加自定义广播内容

```text
TTM:ADD-<Char>
```

- `Char` 为附加广播数据，长度满足 `0 < L <= 16`。
- 掉电保存，仅未连接时可设置。

#### 15. 连接间隔设定

```text
TTM:CIT-<X>ms
```

- `X` 可为：20、50、100、200、300、400、500、1000、1500、2000，单位 ms。
- 成功返回 `TTM:OK\r\n\0`；超时失败返回 `TTM:TIMEOUT\r\n\0`。
- 复位默认连接间隔为 12.5 ms，掉电不保存，仅连接成功后有效。

## 回流焊作业指导

仅适用于无铅作业，仅供参考。

## 包装信息

### 卷带包装

- 采用卷带包装和真空密封袋。
- 每卷 1500 个模块。

### 内箱包装

- 内箱尺寸：352 mm × 348 mm × 56 mm。
- 每个内箱放置 1 卷，即 1500 个模块。

### 外箱包装

- 外箱尺寸：354 mm × 250 mm × 362 mm。
- 每个外箱可放置 4 个内箱，即 6000 个模块。

### 最小起订量

|包装方式|每箱卷带数|每卷模块数|最小起订量|
|---|---:|---:|---:|
|卷带|4 卷|1500 个|1500 个|
