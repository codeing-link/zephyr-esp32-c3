# ESP32-C3-DevKitM 的 GPIO8 LED 闪灯配置。
CONFIG_GPIO=y

# 保持开发板默认 UART0 引脚的串口控制台可用。
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# 日志通过 USB Serial/JTAG（主机侧 /dev/ttyACM0）输出。
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y
CONFIG_LOG_BACKEND_UART=y
CONFIG_LOG_DEFAULT_LEVEL=3
